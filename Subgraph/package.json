{
  "name": "gift-chain",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gift-chain",
    "create-local": "graph create --node http://localhost:8020/ gift-chain",
    "remove-local": "graph remove --node http://localhost:8020/ gift-chain",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gift-chain",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.0",
    "@graphprotocol/graph-ts": "0.37.0",
    "graph": "^0.2.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
